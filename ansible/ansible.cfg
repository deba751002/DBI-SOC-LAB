[defaults]
inventory = inventory.ini
host_key_checking = False
retry_files_enabled = False
timeout = 30

[winrm]
# Ansible controls Windows endpoints over WinRM (5985/5986), not SSH.
# Nothing new listens inbound on the SOC Lab side - Ansible only calls OUT
# to each endpoint, same push model as every deploy script it wraps.
