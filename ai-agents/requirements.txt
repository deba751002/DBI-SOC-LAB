# CrewAI core
# crewai==0.63.6 (this repo's original pin) never actually provided
# crewai.tools.BaseTool - at that version BaseTool lived only in the
# separate crewai-tools package (crewai_tools.tools.base_tool), while every
# agent/tool file here does `from crewai.tools import BaseTool`. This must
# have been broken from day one; it surfaced now because it's this repo's
# first real container start. BaseTool moved into crewai's own core package
# at 0.80.0 - 0.105.0 is the latest 0.x release still on pydantic>=2.4.2
# (matching our pin below; 0.2xx+ requires pydantic>=2.11.9) and no longer
# needs crewai-tools as a dependency at all - confirmed against the actual
# published wheel, not just changelog claims.
crewai==0.105.0

# LangChain + Ollama
langchain==0.2.16
langchain-community==0.2.16
langchain-ollama==0.1.3

# FastAPI server
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# OpenSearch client
opensearch-py==2.6.0

# HTTP
requests==2.32.3
urllib3==2.2.3

# Data
pyyaml==6.0.2
python-dotenv==1.0.1

# Async
anyio==4.4.0
