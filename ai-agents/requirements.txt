# CrewAI core
# crewai-tools is NOT a dependency here - nothing in this codebase imports
# from it (BaseTool comes from crewai.tools, built into the core package).
# It was pulled in unused and pinned crewai-tools==0.12.0, whose lancedb<0.6.0
# constraint has since been removed from PyPI entirely, breaking every fresh
# install. Removed rather than version-bumped, since the dependency itself
# was dead weight.
crewai==0.63.6

# LangChain + Ollama
langchain==0.2.16
langchain-community==0.2.16
langchain-ollama==0.1.3

# FastAPI server
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# OpenSearch client
opensearch-py==2.6.0

# HTTP
requests==2.32.3
urllib3==2.2.3

# Data
pyyaml==6.0.2
python-dotenv==1.0.1

# Async
anyio==4.4.0
